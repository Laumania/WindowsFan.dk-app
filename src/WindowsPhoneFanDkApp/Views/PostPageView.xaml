<phone:PhoneApplicationPage 
    x:Class="WindowsPhoneFanDkApp.Views.PostPageView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="clr-namespace:WindowsPhoneFanDkApp.Controls"
    xmlns:Converts="clr-namespace:WindowsPhoneFanDkApp.Converts"
    xmlns:Controls="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait" Orientation="Portrait"
    mc:Ignorable="d" d:DesignHeight="768" d:DesignWidth="480"
    shell:SystemTray.IsVisible="True">


    <phone:PhoneApplicationPage.Resources>
        <Converts:PostInfoConverter x:Name="authorConverter" />
        <Converts:CommentNameConverter x:Name="commentNameConverter" />
        
        <Style x:Name="StyleinfoLight" TargetType="TextBlock">
            <Setter Property="FontSize" Value="8" />
            <Setter Property="FontWeight" Value="ExtraLight" />
            <Setter Property="FontStyle" Value="Italic" />
            <Setter Property="Foreground" Value="Gray"  /> 
            <Setter Property="Margin" Value="10,0,10,0" />
        </Style>
    </phone:PhoneApplicationPage.Resources>

    <Grid x:Name="LayoutRoot" Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="20*" />
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" >
            <TextBlock Text="wp" Height="45" FontSize="30" FontWeight="ExtraBold"  HorizontalAlignment="Center" VerticalAlignment="Top" FontFamily="/WindowsPhoneFanDkApp;component/Fonts/Harabara.ttf#Harabara" />
            <TextBlock Text="fan" Height="45" FontSize="30"  HorizontalAlignment="Center" VerticalAlignment="Top" FontFamily="sdf" />
        </StackPanel>
        <!--Pivot Control-->
        <Controls:Pivot Name="pivMain" Title="" Grid.Row="1" Margin="0,-30,0,0" >

            <!--Pivot item one-->
            <Controls:PivotItem Header="Read">
                <ScrollViewer>
                    <StackPanel Orientation="Vertical">
                        <StackPanel Orientation="Vertical" Margin="12" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                            <controls:HtmlTextBlock Html="{Binding Title}"/>
                            <TextBlock Text="{Binding Path=PostSignature, Converter={StaticResource authorConverter}}" Style="{StaticResource StyleinfoLight}" />
                            <controls:HtmlTextBlock Html="{Binding Content}" Margin="0,-170,0,0" />
                        </StackPanel>
                        
                    </StackPanel>
                </ScrollViewer>
            </Controls:PivotItem>

            <!--Pivot item two-->
            <Controls:PivotItem Header="Comments">
                <ScrollViewer>
                <ItemsControl ItemsSource="{Binding Comments}" Margin="12" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Vertical" Background="#50D3D3D3" Margin="0,10,0,10">
                                <controls:HtmlTextBlock Html="{Binding Name, Converter={StaticResource commentNameConverter}}" FontWeight="Bold"/>
                                <TextBlock Text="{Binding Date}" Style="{StaticResource StyleinfoLight}" />
                                <controls:HtmlTextBlock Html="{Binding Content}" Margin="0,10,0,0"/>
                            </StackPanel>
                        </DataTemplate>
                     </ItemsControl.ItemTemplate>

                </ItemsControl>
                </ScrollViewer>
            </Controls:PivotItem>
        </Controls:Pivot>
    </Grid>    
 
    <!--Sample code showing usage of ApplicationBar-->
    <phone:PhoneApplicationPage.ApplicationBar>
        <shell:ApplicationBar IsVisible="false" IsMenuEnabled="false">
            <shell:ApplicationBarIconButton IconUri="/Content/appbar.addcomment.rest.png" Text="Kommentér" x:Name="btnAddComment" Click="btnAddComment_Click"/>
            <shell:ApplicationBar.MenuItems>
                <shell:ApplicationBarMenuItem Text="MenuItem 1"/>
                <shell:ApplicationBarMenuItem Text="MenuItem 2"/>
            </shell:ApplicationBar.MenuItems>
        </shell:ApplicationBar>
    </phone:PhoneApplicationPage.ApplicationBar>

</phone:PhoneApplicationPage>
